services:
  - type: web
    name: kayonza-maize-station
    env: python
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run kayonza_web_pro.py --server.port $PORT --server.address 0.0.0.0
    plan: free
